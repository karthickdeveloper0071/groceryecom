/**
 * Identity and access: accounts, roles, login and tokens.
 * Other modules may only use the {@code api} package.
 */
@ApplicationModule(displayName = "Identity & access")
package com.groceryecom.modules.identity;

import org.springframework.modulith.ApplicationModule;
