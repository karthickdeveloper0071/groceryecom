/**
 * Public contract of the identity module: the only types other modules may use.
 * Everything else in this module is internal and off-limits, which
 * {@code ModularityTest} enforces on every build.
 */
@NamedInterface("contract")
package com.groceryecom.modules.identity.contract;

import org.springframework.modulith.NamedInterface;
