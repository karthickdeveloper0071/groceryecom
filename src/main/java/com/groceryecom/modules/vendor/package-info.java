/**
 * Vendors: the stores that sell on the platform, who may act for them, and the
 * approval that lets a store trade.
 * Other modules may only use the {@code contract} package.
 */
@ApplicationModule(displayName = "Vendor")
package com.groceryecom.modules.vendor;

import org.springframework.modulith.ApplicationModule;
